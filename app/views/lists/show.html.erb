<h1>This is your <%= @list.name %> page</h1>


<% @movies.each do |movie| %>
  <div><%=movie.title%><%=link_to "Delete", bookmark_path(@bookmark)%></div>
<% end %>

<%=link_to "Create a bookmark", new_list_bookmark_path(@list) %>
